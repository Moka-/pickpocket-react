import React, { Component } from 'react'
import PropTypes from 'prop-types'

export default class Selector extends Component {
  static propTypes = {
    pocketType: PropTypes.object.isRequired,
  }

  render() {
    return (
      <div>
        fdsfsdfsd
      </div>
    )
  }
}
